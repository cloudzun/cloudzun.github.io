---
import { getCollection } from "astro:content";
import Layout from "@/layouts/Layout.astro";
import Header from "@/components/Header.astro";
import Footer from "@/components/Footer.astro";
import Socials from "@/components/Socials.astro";
import LinkButton from "@/components/LinkButton.astro";
import Card from "@/components/Card.astro";
import getSortedPosts from "@/utils/getSortedPosts";
import IconRss from "@/assets/icons/IconRss.svg";
import IconArrowRight from "@/assets/icons/IconArrowRight.svg";
import { SITE } from "@/config";
import { SOCIALS } from "@/constants";

const posts = await getCollection("blog");

const sortedPosts = getSortedPosts(posts);
const featuredPosts = sortedPosts.filter(({ data }) => data.featured);
const recentPosts = sortedPosts.filter(({ data }) => !data.featured);
---

<Layout>
  <Header />
  <main id="main-content" data-layout="index" class="app-layout">
    <section id="hero" class:list={["pt-8 pb-6", "border-b border-border"]}>
      <h1 class="my-4 inline-block text-4xl font-bold sm:my-8 sm:text-5xl">
        Hi, I'm CloudZun
      </h1>
      
      <p class="text-lg">
        <strong>讲师</strong> · <strong>工程师</strong> · <strong>博主</strong>
      </p>
      
      <p class="mt-4">
        专注于 <strong>AI 协同编程教育</strong> 与 <strong>Agentic AI 工作流</strong> 实践。
        设计并交付 OpenCode Labs、Vibe Coding Camp 等课程体系，
        探索人类与 AI 协作开发的新范式——人类保持架构决策权与质量判断，AI 负责代码执行与方案生成。
      </p>
      
      <p class="mt-2">
        这里是我的个人网站，分享 AI、自动化和开发经验。已发布 
        <LinkButton
          class="underline decoration-dashed underline-offset-4 hover:text-accent"
          href="/posts/"
        >
          115+ 篇技术文章
        </LinkButton>。
      </p>
      
      <p class="mt-2">
        提供 <strong>技术培训</strong> 与 <strong>咨询顾问</strong> 服务，
        <LinkButton
          class="underline decoration-dashed underline-offset-4 hover:text-accent"
          href="/about/"
        >
          查看详情 →
        </LinkButton>
      </p>

      {
        // 社交链接
        SOCIALS.length > 0 && (
          <div class="mt-6 flex max-sm:flex-col sm:items-center">
            <Socials />
          </div>
        )
      }
    </section>

    {
      // 精选文章（手动指定代表作）
      featuredPosts.length > 0 && (
        <section
          id="featured"
          class:list={[
            "pt-12 pb-6",
            { "border-b border-border": recentPosts.length > 0 },
          ]}
        >
          <div class="flex items-center justify-between mb-6">
            <h2 class="text-2xl font-semibold tracking-wide">精选文章</h2>
            <a
              href="/categories/"
              class="text-accent hover:underline text-sm font-medium"
            >
              按分类浏览 →
            </a>
          </div>
          <ul>
            {featuredPosts.map(data => (
              <Card variant="h3" {...data} />
            ))}
          </ul>
        </section>
      )
    }

    {
      recentPosts.length > 0 && (
        <section id="recent-posts" class="pt-12 pb-6">
          <div class="flex items-center justify-between mb-6">
            <h2 class="text-2xl font-semibold tracking-wide">最新文章</h2>
            <div class="flex items-center gap-3">
              <a
                href="/rss.xml"
                class="flex items-center gap-2 px-4 py-2 rounded-lg bg-accent text-white hover:opacity-90 transition-opacity text-sm font-medium"
                title="RSS 订阅"
              >
                <IconRss class="w-4 h-4" />
                <span>RSS 订阅</span>
              </a>
              <a
                href="/archives/"
                class="text-accent hover:underline text-sm font-medium"
              >
                归档 →
              </a>
            </div>
          </div>
          <ul>
            {recentPosts.map(
              (data, index) =>
                index < SITE.postPerIndex && <Card variant="h3" {...data} />
            )}
          </ul>
        </section>
      )
    }

    <div class="my-8 text-center">
      <LinkButton href="/posts/">
        浏览所有文章
        <IconArrowRight class="inline-block rtl:-rotate-180" />
      </LinkButton>
    </div>
  </main>
  <Footer />
</Layout>

<script>
  document.addEventListener("astro:page-load", () => {
    const indexLayout = (document.querySelector("#main-content") as HTMLElement)
      ?.dataset?.layout;
    if (indexLayout) {
      sessionStorage.setItem("backUrl", "/");
    }
  });
</script>
